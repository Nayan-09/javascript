//Test whether the character at a given index is lowercase.
let str = "JavaScript";
let index = 4; // Character at index 4 is 'S'
console.log(str.charAt(index) === str.charAt(index).toLowerCase()); // Output: false (because 'S' is uppercase)
